<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset="UTF-8">
        <title>Add Cookie</title>
    </head>
    <body>
        <div id="form">
            <form action="/add-cookie" method="POST">
                <label>
                    Add Cookie
                    <input type="text" name="cookie_key"/>
                    <input type="text" name="cookie_value"/>
                    <input type="submit" value="Submit"/>
                </label>
            </form>
        </div>
<hr>
        <div id="table">
            <table border="1">
                <caption>Cookies</caption>
                <tr>
                    <th>Key</th>
                    <th>Value</th>
                </tr>

                <% for(let key in cookies){ %>
                    <tr><th><%= key %></th><th><%= cookies[key] %></th></tr>
                    <% } %>
            </table>
        </div>
    </body>
</html>